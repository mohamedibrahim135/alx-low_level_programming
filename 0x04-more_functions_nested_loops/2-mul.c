#include "main.h"
/**
*mul - Multidlies two integers
*@x: The first number to multiply
*@y: The second number to multiply with first number
*Return: 1 for upper letter or 0 for any else
*/
int mul(int x, int y)
{
return (x * y);
}
